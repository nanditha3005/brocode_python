#to calculate the hypotenuese of right angle triangle  c=sqrt(a²+b²)
import math
a=float(input("Enter side A:"))
b=float(input("Enter side B:"))

c=math.sqrt(pow(a,2)+pow(b,2))

print(f"The side C ={c}")

# Enter side A:3
# Enter side B:4
# The side C=5.0